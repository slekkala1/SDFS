package tempest.commands.interfaces;

/**
 * Simple interface to attach to a Command to indicate to the Client that the
 * Command should be sent over TCP
 *
 * This may be nicer if it was an annotation rather than an interface
 */
public interface Udp {
}
